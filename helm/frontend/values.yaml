
replicaCount: 1

image:
  repository: 192.168.1.16/smart-task-management-system/frontend
  tag: v1
  pullPolicy: Always

imagePullSecrets: []

service:
  type: ClusterIP
  port: 80
  targetPort: 80

containerPort: 80

resources: {}

env:
  NODE_ENV: "production"

ingress:
  enabled: true
  className: nginx

  host: frontend.smarttask.local

  path: /

  pathType: Prefix